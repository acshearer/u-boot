if TARGET_SABER_SABER2020

config SYS_BOARD
	default "saber2020"

config SYS_VENDOR
	default "saber"

config SYS_CPU
	default "saber"

config SYS_CONFIG_NAME
	default "saber-saber2020"

config SYS_TEXT_BASE
	default 0x80400000

config DEFAULT_DEVICE_TREE
	default "saber"

config BOARD_SPECIFIC_OPTIONS # dummy
	def_bool y
	select SABER_RISCV

endif
